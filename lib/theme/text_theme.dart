import 'package:flutter/material.dart';

const textColor = Color(0xFF1F2025);
var defaultTextTheme = Typography.blackMountainView
    .apply(fontFamily: "Inter", displayColor: textColor, bodyColor: textColor);
